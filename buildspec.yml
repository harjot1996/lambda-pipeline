version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "No dependencies!"
  build:
    commands:
      - zip -r9 ../index.zip .
      - ls
      - cd ..
      - ls
      - zip -g index.zip index-photos.py
      - ls
  post_build:
    commands:
      - echo "Updating lambdas"
      - ls
      - aws lambda update-function-code --function-name index-photos2 --zip-file fileb://index.zip
      - echo "Updates completed"
